pipeline{
    agent any
    stages{
        stage ('compile'){
        steps{
            script{
                echo "compile the code"
            }
        }
        }
        stage ('compile'){
        steps{
            script{
                echo "compile the code"
            }
        }
    }
        stage ('compile'){
        steps{
            script{
                echo "compile the code"
            }
        }
        }
    }
}